#include <iostream>
#include <stdlib.h>
#include <windows.h>
using namespace std;

int main()
{
    for(int y=0; y<10; y++)
    {
        system("cls");
        for(int c=0; c<=y; c++)
        {
            cout << " ";
        }
        cout << "#";
        Sleep(100);
    }
    return 0;
}
